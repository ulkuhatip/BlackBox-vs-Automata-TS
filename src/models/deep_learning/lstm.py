from __future__ import annotations


class LSTMModel:
    """Placeholder LSTM model wrapper."""

    def fit(self, x_train, y_train, x_val=None, y_val=None):
        _ = (x_train, y_train, x_val, y_val)
        return self

    def predict(self, x_test):
        _ = x_test
        return []
